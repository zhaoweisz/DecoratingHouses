<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="yyk.decoratinghouses">

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ParamsSettingActivity">
        </activity>
        <activity android:name=".PriceSettingActivity">
        </activity>
        <activity android:name=".ProjectSettingActivity">
        </activity>
        <activity android:name=".ProjectUpdateActivity">
        </activity>
    </application>

</manifest>